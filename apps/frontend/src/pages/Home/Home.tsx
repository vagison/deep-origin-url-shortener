import Shortener from '../../components/Shortener/Shortener';

const Home = () => {
  return (
    <div className="main-container home-container">
      <Shortener />
    </div>
  );
};

export default Home;
